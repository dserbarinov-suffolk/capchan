# Python dependencies
numpy
Pillow
pytesseract
python-pptx
matplotlib

# System dependencies (install separately, not via pip):
#   ffmpeg      — frame decoding / sampling      (brew install ffmpeg            | apt install ffmpeg)
#   tesseract   — OCR engine for the default backend (brew install tesseract     | apt install tesseract-ocr)
